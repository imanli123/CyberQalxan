[project]
name = "cyberqalxan"
version = "0.1.0"
description = "Off-Chain Merkle Batching for SIEM log integrity"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "pydantic>=2.10",
    "requests>=2.32",
    "cryptography>=44.0",
    "numpy>=2.2",
    "scikit-learn>=1.6",
]

[tool.setuptools.packages.find]
where = ["."]
